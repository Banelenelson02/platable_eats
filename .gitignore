# Build output — never commit compiled code
target/

# IDE files
.idea/
*.iml
*.iws
*.ipr
.vscode/

# Environment — secrets never go in version control
.env
*.env
application-local.properties

# OS
.DS_Store
Thumbs.db

# Maven wrapper (optional — keep if you want others to build without installing Maven)
# !.mvn/wrapper/maven-wrapper.jar